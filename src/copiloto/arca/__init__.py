"""Talking to ARCA's real padrón, behind the registry Protocol.

Optional and self-contained: install it with `uv sync --extra arca`. Nothing
in the rest of the copilot imports this package, and without a certificate
none of it runs.
"""
